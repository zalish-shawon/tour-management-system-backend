export interface IDivision {
    name: string,
    slug: string,
    thumbnail?: string,
    description?: string,
};

export interface IName {
    name: string,
    email: string,
    age: number,
    password: string,
    isActive: boolean,
    
}